/* generated by Svelte v3.24.0 */
import {
	SvelteComponent,
	append,
	attr,
	create_component,
	destroy_component,
	detach,
	element,
	init,
	insert,
	mount_component,
	noop,
	safe_not_equal,
	space,
	transition_in,
	transition_out
} from "/svelte/internal.js";

import Folder from "/components/Folder.js";
import toFolder from "/components/toFolder.js";

function add_css() {
	var style = element("style");
	style.id = "svelte-1hiiw7j-style";
	style.textContent = "div.svelte-1hiiw7j{display:grid;grid-template-columns:1fr 1fr}";
	append(document.head, style);
}

function create_fragment(ctx) {
	let h2;
	let t1;
	let div;
	let folder0;
	let t2;
	let folder1;
	let current;

	folder0 = new Folder({
			props: {
				folder: toFolder`
+ a
  + node_modules
    + b
      - index.js
      + node_modules
        + c
          - index.js
          + node_modules
            + d
              - index.js
`
			}
		});

	folder1 = new Folder({
			props: {
				folder: toFolder`
+ a
  + node_modules
    + b
      - index.js
    + c
      - index.js
    + d
      - index.js
`
			}
		});

	return {
		c() {
			h2 = element("h2");
			h2.textContent = "⤴ 🆙 Hoisting";
			t1 = space();
			div = element("div");
			create_component(folder0.$$.fragment);
			t2 = space();
			create_component(folder1.$$.fragment);
			attr(div, "class", "svelte-1hiiw7j");
		},
		m(target, anchor) {
			insert(target, h2, anchor);
			insert(target, t1, anchor);
			insert(target, div, anchor);
			mount_component(folder0, div, null);
			append(div, t2);
			mount_component(folder1, div, null);
			current = true;
		},
		p: noop,
		i(local) {
			if (current) return;
			transition_in(folder0.$$.fragment, local);
			transition_in(folder1.$$.fragment, local);
			current = true;
		},
		o(local) {
			transition_out(folder0.$$.fragment, local);
			transition_out(folder1.$$.fragment, local);
			current = false;
		},
		d(detaching) {
			if (detaching) detach(h2);
			if (detaching) detach(t1);
			if (detaching) detach(div);
			destroy_component(folder0);
			destroy_component(folder1);
		}
	};
}

class Component extends SvelteComponent {
	constructor(options) {
		super();
		if (!document.getElementById("svelte-1hiiw7j-style")) add_css();
		init(this, options, null, create_fragment, safe_not_equal, {});
	}
}

export default Component;