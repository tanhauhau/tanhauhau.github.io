/* generated by Svelte v3.24.0 */
import {
	SvelteComponent,
	append,
	attr,
	detach,
	element,
	init,
	insert,
	noop,
	safe_not_equal,
	space
} from "/svelte/internal.js";

function add_css() {
	var style = element("style");
	style.id = "svelte-1gjjrol-style";
	style.textContent = "pre.svelte-1gjjrol{white-space:break-spaces}pre.svelte-1gjjrol:nth-child(2){word-break:break-all}";
	append(document.head, style);
}

function create_fragment(ctx) {
	let h2;
	let t1;
	let pre0;
	let t3;
	let pre1;
	let t5;
	let p;

	return {
		c() {
			h2 = element("h2");
			h2.textContent = "🙈 Impossible?";
			t1 = space();
			pre0 = element("pre");
			pre0.textContent = "semver -> cross-spawn -> execa -> sane -> jest-haste-map -> @jest/transform -> jest-runtime -> jest-jasmine2 -> jest-runner -> @jest/test-sequencer -> jest-config -> @jest/core";
			t3 = space();
			pre1 = element("pre");
			pre1.textContent = "/node_modules/@jest/core/node_modules/jest-config/node_modules/@jest/test-sequencer/node_modules/jest-runner/node_modules/jest-jasmine2/node_modules/jest-runtime/node_modules/@jest/transform/node_modules/jest-haste-map/node_modules/sane/node_modules/execa/node_modules/cross-spawn/node_modules/semver/node_modules";
			t5 = space();
			p = element("p");
			p.textContent = "313 characters";
			attr(pre0, "class", "svelte-1gjjrol");
			attr(pre1, "class", "svelte-1gjjrol");
		},
		m(target, anchor) {
			insert(target, h2, anchor);
			insert(target, t1, anchor);
			insert(target, pre0, anchor);
			insert(target, t3, anchor);
			insert(target, pre1, anchor);
			insert(target, t5, anchor);
			insert(target, p, anchor);
		},
		p: noop,
		i: noop,
		o: noop,
		d(detaching) {
			if (detaching) detach(h2);
			if (detaching) detach(t1);
			if (detaching) detach(pre0);
			if (detaching) detach(t3);
			if (detaching) detach(pre1);
			if (detaching) detach(t5);
			if (detaching) detach(p);
		}
	};
}

class Component extends SvelteComponent {
	constructor(options) {
		super();
		if (!document.getElementById("svelte-1gjjrol-style")) add_css();
		init(this, options, null, create_fragment, safe_not_equal, {});
	}
}

export default Component;