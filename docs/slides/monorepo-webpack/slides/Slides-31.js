/* generated by Svelte v3.24.0 */
import {
	SvelteComponent,
	detach,
	element,
	init,
	insert,
	noop,
	safe_not_equal,
	space
} from "/svelte/internal.js";

function create_fragment(ctx) {
	let h2;
	let t1;
	let p;
	let t5;
	let ul;

	return {
		c() {
			h2 = element("h2");
			h2.textContent = "webpack";
			t1 = space();
			p = element("p");
			p.innerHTML = `webpack’s <code>enhanced-resolve</code> implements node resolution algorithm + niceties, eg:`;
			t5 = space();
			ul = element("ul");

			ul.innerHTML = `<li>alias</li> 
<li>custom extensions</li> 
<li>custom main field</li> 
<li>custom module folder</li>`;
		},
		m(target, anchor) {
			insert(target, h2, anchor);
			insert(target, t1, anchor);
			insert(target, p, anchor);
			insert(target, t5, anchor);
			insert(target, ul, anchor);
		},
		p: noop,
		i: noop,
		o: noop,
		d(detaching) {
			if (detaching) detach(h2);
			if (detaching) detach(t1);
			if (detaching) detach(p);
			if (detaching) detach(t5);
			if (detaching) detach(ul);
		}
	};
}

class Component extends SvelteComponent {
	constructor(options) {
		super();
		init(this, options, null, create_fragment, safe_not_equal, {});
	}
}

export default Component;